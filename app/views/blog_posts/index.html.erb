<h1>Blog Posts</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Content</th>
      <th>Publish date</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% if  !@blog_posts.empty?
         @blog_posts.each do |blog_post|
    %>
      <tr>
        <td><%= blog_post.title %></td>
        <td><%= blog_post.content%> <br> <%= Time.new(2019, 06, 01, 12, 45, 0, "+03:00") %></td>
        <td><%= blog_post.publish_date %></td>
        <td><%= link_to 'Show', blog_post %></td>
        <td><%= link_to 'Edit', edit_blog_post_path(blog_post) %> </td>
        <td><%= link_to 'Destroy', blog_post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end #loop
       else
    %>
      <tr>
         <td> Данные не найдены</td><td></td><td></td><td></td><td></td><td></td>
       </tr>
    <% end # if empty?%>
  </tbody>
</table>

<br>

<%= link_to 'New Blog Post', new_blog_post_path, class: "btn btn-primary" %>
